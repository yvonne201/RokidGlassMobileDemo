<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rokid.alliancedemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/demo_app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DemoAudioRecordActivity"></activity>
        <activity android:name=".DemoCaptureActivity" />
        <activity android:name=".DemoRKOnlineActivity" />
        <activity android:name=".DemoFacePlateActivity" />
        <activity android:name=".DemoPresentationActivity" />
        <activity android:name=".DemoGlassActivity" />
        <activity android:name=".DemoRKOfflineActivity"/>
        <activity android:name=".DemoRKOfflinePlateActivity"/>
        <activity android:name=".SimpleDemoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>